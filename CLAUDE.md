# Sat the Fighters - 案件管理アプリ

有限会社サット（映像制作会社）向け案件管理アプリ。スマートフォン向けゲーム風UI。

## プロジェクト構成

**単一HTMLファイル構成** — `案件管理アプリ.html` 1ファイルに全てのコード（HTML/CSS/JS）が完結。

```
案件管理アプリ/
├── 案件管理アプリ.html      ← 本体（編集対象）
├── CLAUDE.md                ← このファイル
└── backups/                 ← JSONバックアップ置き場（推奨）
```

## ユーザーについて

- プログラミング初心者（Claudeに自然言語で指示する形で開発）
- Mac環境で開発、ファイルはデスクトップ配下に配置
- アプリはブラウザで `file://` 直開きで動作
- 日本語でやり取り、難しい用語は避ける

## 開発ルール

- **編集は `案件管理アプリ.html` 1ファイルのみ**（HTML/CSS/JSすべてインライン）
- **編集後は必ずJSの構文チェック**してから完了する（`node --check` 等）
- 大きな変更後は localStorage キーの破壊的変更がないか確認
- 動作確認時は localStorage クリアで初期状態をテストできる
- レイアウトは **iPhone想定の phone-frame** が基本、デスクトップは @media クエリで対応
- 機能の更新追加修正などをした時は更新した場所以外もちゃんと機能しているからテストしてから完了とすること

## ⚙️ 変更後の必須チェック工程（ユーザー要望 2026-05-27）

**コードを書き直したら、報告前に必ず以下の工程を踏むこと。**

1. **JS構文チェック**：`node --check` で確文エラーがないか確認（必須）
2. **影響範囲の確認**：
   - 変更した関数を呼んでいる他の場所（grep）をチェック
   - 関連する画面（パネル・モーダル・タブ）の表示が崩れていないか
   - z-index・position・transform の組み合わせで他要素を覆っていないか
   - **UI検証マトリクス（必須・ユーザー要望 2026-06-11）**：表示に関わる変更は
     「スマホ幅(390px)／デスクトップ幅」×「文字サイズ100%／150%（初期値）」の
     **4パターン全部**をブラウザ実機で確認する。拡大時（〜180%）の数字・ボタンのはみ出し/重なりを特に見る
     （文字サイズは localStorage の `font_scale` で切替。初期値1.5・範囲0.8〜2.2。固定px幅の列は `--fs-scale` 変数で拡大に追従させる）
3. **既存機能との衝突確認**：
   - イベントリスナーの重複登録なし
   - グローバル関数の上書きなし（既存名と被ったら別名にする）
   - localStorage キーの破壊的変更なし
4. **バグの自己レビュー**：
   - undefined / null になり得る箇所
   - 非同期処理（setTimeout・confirm dialog）の戻り値依存箇所
   - レスポンシブ（モバイル/デスクトップ両方）で破綻しないか
5. **報告時に明示**：「✅ 構文チェックOK」「✅ 関連箇所〇〇を確認」など、何を確認したかを書く
6. **Gitへpush（必須・ユーザー要望 2026-06-07）**：修正・変更が完了したら、毎回 `git add` → `git commit` → `git push` まで自動で行う。ユーザーに「pushして」と言われなくても必ず実施し、コミットハッシュとpush結果を報告する。

> ⚠️ 「動くはず」で報告しない。確認した工程を必ず明記する。
> ⚠️ 修正したら毎回 push まで完了させる（聞き直さない）。

## 主な機能・タブ構成

ナビバー5タブ（2026-07-11 実態に合わせて更新）：
1. **🏠 ホーム** — ユーザーステータスカード、アクションボタン、月間カレンダー、進行状況カード
2. **🏦 会計室** — 書類作成（受注ノート/ワークシート/請求書/テンプレート/見積書/納品書）＋集計（売上表/受注番号の変更/重複の整理）
3. **📁 管理室** — 簡易ワークシート/機材管理/取引先管理/商品リスト/メディア管理/運転記録表/削除した書類（ゴミ箱）
4. **✊ 漢たち** — スタッフ10名（静的HTML、ドット絵アバター）
5. **🏟️ SUMO** — ミニゲーム（10力士の対戦、TV中継風UI）

その他主要画面：
- 機材マスター編集（青ヘッダー、案件編集画面から開く）
- データ管理画面（上部「データ管理」ボタンから開く・合言葉/再送/バックアップ/復元/リセット）
- ⚠️ `tab-2 スケジュール` はナビに項目が無い孤立タブ（ホームカレンダーと重複・整理候補 2026-07-11）

### 2026-07-08〜11 の主な仕様変更（要点）
- **外注費はワークシートで一元管理**：請求書の外注費記入欄・明細の「外注」列を廃止。売上の外注費集計はWS（外注リスト＋レンタル）のみ参照（二重計上防止）
- **印刷/PDF時は明細を「保存のみ」**（発行＝請求済にはしない・B案）。印刷したのに未保存で消える事故の防止
- **書類の空行自動追加を廃止**（見積/請求/納品共通）：最低10行の水増しとフッター孤立回避の空行を撤去。実明細のみで印刷（手動スペーサーは維持）
- **同期の明細保護ガード**：`_pickNewer` に「明細ゼロの新版が明細あり請求書を丸ごと上書きするのを防ぐ」保護（ガリガリくんT26075事故対策）。**残課題：GAS側は純LWWのまま**（空版がシートを直接上書きする瞬間は防げない）
- **売上PDFは請求済みのみ**（月別・全体とも）＋削除済み案件（deletedWS）の残存請求書を除外
- **売上表**：月グループは常に新しい順固定、月内は並び順セレクト（既定＝請求日の古い順）。粗利に利益率（粗利÷請求額）を案件行・サマリー両方に表示。部門別粗利は精度向上まで非表示
- **ダッシュボード**：受注分類に「撮影」を含む案件のみ表示（スチール撮影も含む）
- **表記**：「撮影日程」→「日程」（**同期の列名『撮影日程』は変更していない**）。「請求書を発行（案件終了）」に文言統一
- **使用車両**：複数選択ボタン（1号車/2号車/城間号/平岡号）＋自由記入（「／」区切り保存・旧単一値と互換）
- **掃除（2026-07-11）**：デッドコード18関数・約280行を削除（旧行追加方式 addInvLine 等、外注費UI一式、未使用ラッパー）。保存/発行/印刷時に外注費リストを空上書きするバグを修正

## 重要な命名規則

- **案件番号**：`T26045` 形式（接頭辞 + 西暦下2桁 + 連番3桁）。接頭辞は **T=技術部（撮影・音声）／E=編集／P=制作（演出）** から選択（2026-06-16にD→Pへ変更）。連番は《接頭辞×年》ごとに独立し**年が変わると自動で001にリセット**（getNextOrderNum、例 2027年最初=T27001）。`getNextOrderNum` は ws_/projects_saved に加え **inv_キーも走査**して採番するので、ワークシートと請求書で番号が被らない（2026-06-16）。番号は英数字とハイフンのみ（記号は登録時に自動除去）
  - **【再発防止・2026-06-25】受注番号の他端末との衝突（＝同期での上書き事故）対策**：新規案件(`registerNewProject`)・新規単独請求書(`saveInvDoc` の `_invIsNew`)は、保存直前に `_verifyOrderNumFreeRemote(num, prefix)` を呼び、シート最新の order/invoice を pull で取り込んでから重複判定する。被っていたら空き番号へ自動繰り上げ＋警告して保存をブロック（オフライン時はローカル判定にフォールバック）。これは自端末だけ見て採番していた旧挙動（T26076/T26043の上書き事故の原因）の対策。GAS再デプロイ不要のクライアント側対策。**残課題**：複数端末が未送信のまま同番号を同時生成するケースは防げない（完全防止にはGAS側の一意性チェックが必要）
- **単独書類の番号**：**新規で請求書だけを作る場合も T/E/P の受注番号を設定**できる（請求書編集画面・自社担当の下の受注番号欄。接頭辞ボタン＋番号、次の空き番号を自動入力・編集可。新規時のみ編集可で保存後はロック。重複は保存時に警告でブロック）。inv_T26xxx 等で保存され売上表にも計上される（_collectSalesItems は `^(INV-\d|[TEP]\d)` を対象）。旧 `INV-26001`/`EST-`/`DLV-` 形式のデータも引き続き有効（INV-は売上対象、EST-/DLV-は対象外）。納品書化(createDeliveryFromCurrent)は従来どおり DLV- 採番
- **商品コード**：`Z020`、`Z104` 等（機材ごとの請求コード）
- **localStorageキー**：
  - `ws_${num}` — 各ワークシートの保存データ
  - `inv_${num}` — 請求書データ
  - `projects_saved` — 保存済み案件リスト（JSON配列）
  - `deleted_ws` — 削除済みワークシート番号
  - `equip_master` — 機材マスタ（JSON配列）
  - `last_login_name`, `last_login_time` — ログイン状態
  - `last_backup_time` — 最終バックアップ日時（ISO文字列）

## 主要データ構造

### USERS（ログイン情報）
```javascript
const USERS = { shin: '0000' }; // ユーザー名(小文字): 4桁PIN
```

### USER_STAFF（ログインユーザーのスタッフ情報）
```javascript
const USER_STAFF = {
  shin: {
    name: '城間 心', role: 'C / CA カメラマン',
    projects: 4, hours: 168, overtime: 22,
    level: 38, exp: 1240, expNext: 1500,
    str: 72, spd: 85, end: 68,
    skills: ['📷 速写', '🎯 ピンポイント', '🌅 マジックアワー'],
    avatar: '<svg>...</svg>'
  }
};
```

### SUMO_FIGHTERS（相撲ミニゲームの力士）
```javascript
{ name, level, str, spd, end, palette:{hair, body, bg, hat, glasses, headphones} }
```

`makePixelChar(palette)` 関数で16x16のドット絵SVGを生成。10名分定義済み。

### 機材マスタ（equip_master）
```javascript
{ id:'em001', cat:'📷 カメラ', name:'SONY PXW-NX5R ①', code:'Z020', qty:1, price:30000 }
```

## スタッフ一覧（10名）

漢たちタブで静的HTMLとして実装。SUMO_FIGHTERSと名前を揃える。

1. 川崎 哲也（P/D・リーダー・銀ヘルメット）
2. 上原 健二（C・キャップ）
3. 平岡 誠（CA・茶髪）
4. 川野 大輔（PM・メガネ）
5. 城間 心（C/CA・赤バンダナ）← ログインユーザー shin に対応
6. 新崎 博（S・ヘッドホン）
7. 玻座間 光（CA休暇・ピンク髪）
8. 森田 直人（E・メガネ）
9. 上地 剛（CA若手・金髪）
10. 松田 義明（D・ベレー帽）

## 既知の制約・課題

### 🟡 デスクトップ表示（2026-06-09にほぼ解消）
- api-bar(54px)/scene-area/scroll-content/オーバーレイの位置は54px基準で統一済み。`applyLayout()`のJS値もCSSと一致させた
- ブレークポイントはデスクトップ`min-width:769px`／モバイル`max-width:768px`に統一（売上表・ws-end-gridも統一済み）
- **新しい全画面オーバーレイを追加する時は、`@media (min-width: 769px)` と `(min-width: 1200px)` の `#xxx-screen` ルール一覧に追加必須**（top:54px/left:200px）

### localStorage の容量制限
- 全データJSONバックアップ機能で対応中（データ管理画面）
- 3日に1回の自動バックアップが動作（起動2秒後にチェック）

## 進行中・予定の作業

### ✅ Google スプレッドシート同期（フェーズ2）— 完了（2026-06上旬）
統合スプレッドシート＋GAS Web App で9種類のデータを同期中（下の kind 一覧参照）。
起動時取り込み＋編集後自動送信＋失敗分は `sync_pending` キューで自動再送
（起動時・5分ごと・ネット復帰時。2026-06-12追加）。GitHub Pages でホスティング済み。

### 将来の予定
- Googleログイン（OAuth）— 実装済みだが Google Cloud Console の「承認済みJavaScript生成元」への
  オリジン登録待ち（error 400: origin_mismatch）。ユーザー側の設定作業
- 案件ステータス管理（受注中/撮影前/編集中/完了/請求済）— 未実装（請求済/入金済は実装済み）
- SUMOトーナメント機能 — 未実装（単発対戦のみ実装済み）
- 経験値・レベルアップシステム — 未実装（表示は固定値）

## ⚠️ 新機能追加時の確認ルール（2026-06-06 追加）

新しいデータを扱う機能（一覧・編集・記録など）を実装したら、作業完了報告の前に必ず確認する：

> **「この機能のデータを他の端末と同期できるようにしますか？　統合スプレッドシートに新しいシートを追加しますか？」**

### 同期の追加方法（実装手順）

同期を追加すると決まった場合、以下の3箇所を更新する：

1. **`docs/superpowers/unified-sheet-Code.gs`** の `KINDS` に1行追加：
   ```javascript
   kindName: { tab: 'シート名', key: 'キー列名', headers: ['キー列名','データ','更新日時','削除フラグ'] }
   ```

2. **`index.html`** の `SYNC_KINDS` に設定を追加：
   ```javascript
   kindName: {
     keyField: 'id', keyHeader: 'id',
     loadLocal: () => loadXxx(),
     saveLocal: (list) => saveXxx(list),
     toRow: (rec) => _jsonToRow(rec, 'id', 'id'),   // ネスト構造ありなら jsonToRow
     fromRow: (row) => { const r = _jsonFromRow(row); r.id = String(row['id'] || r.id || ''); return r; },
     redraw: () => { try { _rerenderIfOpen('xxx-screen', renderXxx); } catch(e) {} }
   }
   ```

3. **`index.html`** の `SYNC_ENABLED` 配列に `'kindName'` を追記

4. ユーザーに「GASを再デプロイしてください」と伝える（GASのコードが変わった場合）

### 現在の同期対象 kind 一覧

| kind | シート名 | キー | 追加日 |
|------|----------|------|--------|
| order | 案件 | 受注No | 既存 |
| child | 子 | 受注No | 既存 |
| invoice | 請求書 | 受注No | 既存 |
| equip | 機材 | id | 既存 |
| client | 取引先 | 取引先コード | 既存 |
| product | 商品 | 商品コード | 既存 |
| media | メディア | ID | 既存 |
| staff | スタッフ | キー | 既存 |
| simpleWS | 簡易ワークシート | id | 2026-06-06 |

## 技術的注意点

- `image-rendering: pixelated` でドット絵をシャープに表示
- `shape-rendering: crispEdges` でSVGのピクセルアートを描画
- 印刷時は `print-color-adjust: exact` でザブトン色を保持
- 案件番号は `T26xxx` 形式。旧 T26xxx→Z26xxx の自動変換（migrateStorageKeys）は廃止済み（受注ノートと整合させるため T が正）
- スタッフ・力士のアバターはSVGを `makePixelChar(palette)` で生成（共通関数）

### 💴 金額計算の正（2026-06-12確定・全箇所この式に統一すること）
- **消費税**：課税対象の明細のみ10%。`taxBase = max(0, 課税対象計 − 値引き)`、`tax = Math.floor(taxBase × 0.1)`、`合計 = (小計 − 値引き) + tax`（calcInvTotal が基準。_calcCaseSales / renderInvCards / ゴミ箱表示も同式）
- **値引き**：`status.discount` に保存（saveInvDoc / _issueInvoiceExec）。openInv 系で復元
- **外注費**：「実際に支払う税込額」で売上から控除（ユーザー決定 2026-06-12）。WS外注リスト・請求書の外注費リスト・請求書明細の外注行は各行に**課税/非課税の選択あり**（2026-06-16）。課税＝税抜入力で ×1.1、非課税＝そのまま加算。レンタルは課税扱い。`_calcCaseSales` は `osTaxableBase`(×1.1)と`osExemptSum`(そのまま)に振り分けて合算
  - 後方互換：WS外注リストの旧データ（taxable無し）は課税扱い、請求書の外注費リストの旧データ（taxable無し）と旧・単一値 `outsourceExtra` は非課税扱い（＝従来の数字を変えない）
- **受注金額（wsd-order-amount）**：**税別（本体価格）**で入力（2026-06-16）。請求書が無い案件の売上は `total = 受注額 + floor(受注額×0.1)` で税込換算して集計（請求済案件の税込と揃える）
- **明細の収集**：保存も発行も taxable / outsource / code を必ず保持する（落とすと非課税が課税に戻る事故になる）
- 印刷用HTML（buildDocHTML）に流す文字列は必ず `escapeHtml()` を通す

### 📄 ワークシートと請求書の明細は「切り離す」（ユーザー要望 2026-07-07）
- **請求書の明細は、ワークシートから自動生成しない**。`_openBlankInvFromWS` で保存明細（`status.lines`）が無い案件を開いたときは、**空の請求書（項目・金額とも空／¥0）**で開き、明細はその画面で**手入力**する。
  - 旧仕様（撤去済み）：クルーを単価一律¥10,000＋機材実額で自動生成していた。これが「受注金額だけ入れて請求済にした案件（例 ガリガリくん T26075：受注550,000→記録¥605,000）」を開くと実額と全く違う金額（¥162,800等）に化ける不整合の原因だった。
  - **明細なしで請求済になる経路**：売上表の「入金済/請求済」チェック（`toggleSalesPaidIn`）は `billed=true` にするが `lines` を作らない。この案件の売上額は `_collectSalesItems` のフォールバック（`受注金額(税別)×1.1`）で計上される。請求書側は空・売上側は受注金額、と役割を分けている（＝金額の二重管理を避け、請求書はゼロから作る）。
  - 空の請求書を保存しても `_collectInvLines` が空行を除外するため `status.lines=[]` となり、売上表のフォールバック額は保持される（回帰なし）。
  - 「押した時だけWSから取り込む」任意ボタンは**現時点で不要**（ユーザー判断 2026-07-07）。復活させる場合は“勝手に入れず、明示操作でのみ取り込む”形にすること。

## コミュニケーション

- ユーザーは技術用語より「動作・見た目の変化」で会話する
- コードを見せる時は最小限、変更点を箇条書きで明示すると伝わりやすい
- 「次は何をしますか？」と聞かれたら、優先度別に2〜4個の選択肢を提示

---

最終更新：2026-07-11
