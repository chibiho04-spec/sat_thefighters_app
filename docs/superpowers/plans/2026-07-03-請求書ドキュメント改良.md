# 請求書ドキュメント改良（空行・行備考・3書類の見分け）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 請求書ドキュメントに「明細の空行」「行ごとの備考」「請求書/見積書/納品書の見分け（色帯＋アイコン＋バッジ・白黒対応）」を追加する。

**Architecture:** 単一HTMLファイル `index.html` にインライン追加。明細1行に `note`（備考）と `spacer`（空行）を追加し、隠しテーブル `#inv-lines-body` の各行の `dataset.note`/`dataset.spacer` に保管。カードUI・編集モーダル・印刷HTML(`buildDocHTML`)・テンプレの各層を更新。3書類の見分けは `buildDocHTML` の最上部に色帯＋バッジ＋インラインSVGアイコンを差し込む。

**Tech Stack:** プレーン HTML/CSS/JS。テストは (1) `<script>` 構文チェック、(2) ローカルサーバ + ブラウザ console 検証（`window.__x` に格納しポーリング）。

**確認済みアンカー（index.html）:**
- `renderInvLines`(10091)、`_readInvLineRows`(10153)、`renderInvLineCards`(10180)、`_esc`(10218)、`delInvLineCard`(10225)、`openInvLineModal`(10240)、`saveInvLineModal`(10277)、`_createInvLineRow`(10311)、`_writeInvLineRow`(10333)、`calcInvTotal`(10355)
- `_collectInvLines`(9581 付近・`function _collectInvLines()`)
- モーダルHTML `#inv-line-modal`(6817-6864)。摘要 textarea は `#inv-modal-name`(6827)、末尾フィールドは外注トグル(6854-6857)
- `buildDocHTML`(14372)。`_T`ラベル(14373-14377)、明細ループ(14402-14434、`if(!name)return;`は14422)、空行補充(14442-14446)、返却HTML先頭(14460「1行目：空白」14463-14464)、タイトル帯(14503-14507)、金額ブロック(14526-14531 border `#2980b9`)、thead 備考`<th width:60px>`(14543)
- テンプレ：`saveCurrentAsTemplate`(9631 付近)、`createInvoiceFromTemplate`(9749 付近)

**共通検証（構文チェック）:**
```bash
cd "/Users/shiromashin/Desktop/claudecode/sat/案件管理アプリ" && node -e 'const fs=require("fs");const h=fs.readFileSync("index.html","utf8");const re=/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/gi;let m,b=[];while((m=re.exec(h)))b.push(m[1]);let e=0;b.forEach((x,i)=>{try{new Function(x)}catch(err){e++;console.log("BLOCK",i,err.message)}});console.log(e?("FAIL "+e):"OK")'
```
期待：`OK`

**共通検証（ブラウザ）:** ローカルサーバを起動し `http://localhost:8793/index.html` を開く。関数は全て `window.` 直下。
```bash
cd "/Users/shiromashin/Desktop/claudecode/sat/案件管理アプリ" && (lsof -ti:8793 >/dev/null 2>&1 || (nohup python3 -m http.server 8793 >/tmp/sat8793.log 2>&1 &)); sleep 1; curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8793/index.html
```

---

### Task 1: データ層（note / spacer の保管・読み取り・空行挿入）

**Files:** Modify `index.html`（`_writeInvLineRow` 10333、`_readInvLineRows` 10153、`_collectInvLines`、`renderInvLines` 10091、`delInvLineCard` 直後に `insertSpacerAfter` 追加）

- [ ] **Step 1: `_writeInvLineRow` で note を dataset に保存**

`index.html:10333-10345` の `_writeInvLineRow` の先頭（`const codeEl = ...` の前）に1行追加：
```javascript
  function _writeInvLineRow(tr, d) {
    tr.dataset.note = d.note || '';
    const codeEl = tr.querySelector('.inv-td-code');
```
（以降は既存のまま）

- [ ] **Step 2: `_readInvLineRows` で note / spacer を読む**

`index.html:10155` の `document.querySelectorAll('#inv-lines-body .inv-line-row').forEach(tr => {` の**直後**に、spacer 早期returnを追加し、通常行の push に note を足す。ループ本体を次に置換（10155 の forEach 内・`out.push({...})` まで）：
```javascript
    document.querySelectorAll('#inv-lines-body .inv-line-row').forEach(tr => {
      if (tr.dataset.spacer === '1') {
        out.push({ code:'', name:'', unit:'', qty:0, price:0, taxable:true, outsource:false, note:'', spacer:true });
        return;
      }
      const nameEl  = tr.querySelector('.inv-line-name') || tr.querySelector('.inv-td-name span');
      const unitEl  = tr.querySelector('.inv-line-unit') || tr.querySelector('.inv-td-unit span');
      const qtyEl   = tr.querySelector('.inv-line-qty')  || tr.querySelector('.inv-td-qty span');
      const priceEl = tr.querySelector('.inv-line-price');
      const priceTextEl = tr.querySelector('.inv-td-price span');
      const codeEl  = tr.querySelector('.inv-td-code');
      const taxEl   = tr.querySelector('.inv-line-tax');
      const taxTextEl = tr.querySelector('.inv-td-tax span');
      const outEl   = tr.querySelector('.inv-line-outsource');
      const outTextEl = tr.querySelector('.inv-td-outsource span');
      const name = (nameEl?.value ?? nameEl?.textContent ?? '').trim();
      const unit = (unitEl?.value ?? unitEl?.textContent ?? '').trim();
      const qty  = Number(String(qtyEl?.value ?? qtyEl?.textContent ?? '').replace(/[^\d.]/g, '')) || 0;
      const price = Number(String(priceEl?.value ?? (priceTextEl?.textContent ?? '')).replace(/[¥,]/g, '')) || 0;
      const code = (codeEl?.textContent ?? '').trim();
      const taxable = taxEl ? (taxEl.value === '1')
                            : (taxTextEl ? taxTextEl.textContent.trim() !== '非課税' : true);
      const outsource = outEl ? !!outEl.checked
                              : (outTextEl ? outTextEl.textContent.trim() === '✓' : false);
      out.push({ code, name, unit, qty, price, taxable, outsource, note: (tr.dataset.note || ''), spacer:false });
    });
```

- [ ] **Step 3: `_collectInvLines` で note / spacer を読み、空行を残す**

`_collectInvLines`（`function _collectInvLines()`）の `tbody.querySelectorAll('.inv-line-row').forEach(tr => {` ループを次に置換（spacer早期push＋除外条件変更＋note追加）：
```javascript
    tbody.querySelectorAll('.inv-line-row').forEach(tr => {
      if (tr.dataset.spacer === '1') {
        lines.push({ code:'', name:'', qty:0, unit:'', price:0, taxable:true, outsource:false, note:'', spacer:true });
        return;
      }
      const nameEl  = tr.querySelector('.inv-line-name') || tr.querySelector('.inv-td-name span');
      const unitEl  = tr.querySelector('.inv-line-unit') || tr.querySelector('.inv-td-unit span');
      const qtyEl   = tr.querySelector('.inv-line-qty')  || tr.querySelector('.inv-td-qty span');
      const priceEl = tr.querySelector('.inv-line-price');
      const priceTextEl = tr.querySelector('.inv-td-price span');
      const codeEl  = tr.querySelector('.inv-td-code');
      const taxEl   = tr.querySelector('.inv-line-tax');
      const outsourceEl = tr.querySelector('.inv-line-outsource');
      const name  = (nameEl?.value  || nameEl?.textContent  || '').trim();
      const unit  = (unitEl?.value  || unitEl?.textContent  || '').trim();
      const qty   = Number((qtyEl?.value || qtyEl?.textContent || '').replace(/[^\d.]/g, '')) || 0;
      const rawPrice = (priceEl?.value || (priceTextEl?.textContent || '').replace(/[¥,]/g, ''));
      const price = Number(rawPrice) || 0;
      const code  = (codeEl?.textContent || '').trim();
      const taxable = taxEl ? (taxEl.value === '1') : true;
      const outsource = outsourceEl ? !!outsourceEl.checked : false;
      if (!name && !price) return;
      lines.push({ code, name, qty, unit, price, taxable, outsource, note: (tr.dataset.note || ''), spacer:false });
    });
```

- [ ] **Step 4: `renderInvLines` で note を dataset に、spacer 行を描画**

`index.html:10094` の `savedLines.forEach((line, i) => {` ブロックの、`const tr = document.createElement('tr'); tr.className='inv-line-row';` の直後に spacer 分岐を挿入し、通常行の後で note を保存する。`savedLines.forEach` 本体の先頭〜`tbody.appendChild(tr);` を次で置換：
```javascript
    savedLines.forEach((line, i) => {
      const tr = document.createElement('tr');
      tr.className = 'inv-line-row';
      if (line.spacer) {
        tr.dataset.spacer = '1';
        tr.innerHTML = `<td class="inv-td" colspan="9" style="height:6px;background:#fafafa;"></td>`;
        tbody.appendChild(tr);
        return;
      }
      tr.dataset.note = line.note || '';
      tr.innerHTML = `
        <td class="inv-td inv-td-code">${line.code}</td>
        <td class="inv-td inv-td-name">
          ${readonly
            ? `<span style="font-size:11px;">${line.name}</span>`
            : `<input class="inv-line-input inv-line-name" value="${line.name}" oninput="calcInvTotal()">`}
        </td>
        <td class="inv-td inv-td-unit">
          ${readonly
            ? `<span style="font-size:11px;">${line.unit}</span>`
            : `<input class="inv-line-input inv-line-unit" value="${line.unit}" style="width:36px;" oninput="calcInvTotal()">`}
        </td>
        <td class="inv-td inv-td-qty">
          ${readonly
            ? `<span style="font-size:11px;">${line.qty}</span>`
            : `<input class="inv-line-input inv-line-qty" type="number" value="${line.qty}" style="width:44px;" oninput="calcInvTotal()">`}
        </td>
        <td class="inv-td inv-td-price">
          ${readonly
            ? `<span style="font-size:11px;">¥${Number(line.price).toLocaleString()}</span>`
            : `<input class="inv-line-input inv-line-price" type="number" value="${line.price}" oninput="calcInvTotal()">`}
        </td>
        <td class="inv-td inv-td-amount" id="inv-amount-${i}">
          ¥${(line.qty * line.price).toLocaleString()}
        </td>
        <td class="inv-td inv-td-tax">
          ${readonly
            ? `<span style="font-size:11px;">${line.taxable === false ? '非課税' : '課税'}</span>`
            : `<select class="inv-line-tax" onchange="calcInvTotal()" style="font-size:11px;padding:2px;">
                 <option value="1" ${line.taxable === false ? '' : 'selected'}>課税</option>
                 <option value="0" ${line.taxable === false ? 'selected' : ''}>非課税</option>
               </select>`}
        </td>
        <td class="inv-td inv-td-outsource" style="text-align:center;">
          ${readonly
            ? `<span style="font-size:11px;">${line.outsource ? '✓' : ''}</span>`
            : `<input type="checkbox" class="inv-line-outsource" ${line.outsource ? 'checked' : ''} onchange="calcInvTotal()" title="この行を外注費として計上する" style="width:16px;height:16px;cursor:pointer;">`}
        </td>
        ${readonly ? '' : `<td class="inv-td"><button class="inv-del-btn" onclick="this.closest('.inv-line-row').remove(); calcInvTotal(); renderInvLineCards();">✕</button></td>`}
      `;
      tbody.appendChild(tr);
    });
```
（変更点：spacer分岐追加、`tr.dataset.note` 設定、削除ボタンに `renderInvLineCards()` 追記。他は既存同一）

- [ ] **Step 5: `insertSpacerAfter(i)` を追加**

`delInvLineCard`（10225-10230）の直後に追加：
```javascript
  function insertSpacerAfter(i) {
    if (_invReadonly) return;
    const rows = _readInvLineRows();
    const at = (typeof i === 'number' && i >= 0) ? i + 1 : rows.length;
    rows.splice(at, 0, { code:'', name:'', unit:'', qty:0, price:0, taxable:true, outsource:false, note:'', spacer:true });
    const disc = Number(document.getElementById('inv-discount')?.value || 0);
    renderInvLines([], rows, false, disc);
    calcInvTotal();
  }
```

- [ ] **Step 6: 構文チェック**

Run（共通の検証コマンド）→ Expected: `OK`

- [ ] **Step 7: ブラウザでデータ層を確認**

サーバ起動＋読み込み後、console：
```javascript
window.__t1='run';(async()=>{ const r={};
  try{
    _openBlankInvScreen('create');
    // 通常行を1つ入れる（隠しテーブルへ直接）
    renderInvLines([], [
      {code:'',name:'撮影費',unit:'式',qty:2,price:5000,taxable:true,outsource:false,note:'メモA'},
      {spacer:true},
      {code:'',name:'編集費',unit:'式',qty:1,price:8000,taxable:false,outsource:false,note:''}
    ], false, 0);
    const rows=_readInvLineRows();
    r.count=rows.length;
    r.row0=rows[0]; r.row1IsSpacer=rows[1].spacer; r.row2=rows[2];
    const collected=_collectInvLines();
    r.collectedCount=collected.length;                 // 3（空行も残る）
    r.collectedHasSpacer=collected.some(l=>l.spacer);  // true
    r.note0=collected[0].note;                         // メモA
    // 挿入
    insertSpacerAfter(0);
    r.afterInsertCount=_readInvLineRows().length;      // 4
    r.afterInsertRow1Spacer=_readInvLineRows()[1].spacer; // true
    try{closeInv();}catch(e){}
  }catch(e){ r.error=String(e&&e.stack||e); }
  window.__t1=r;
})();'started'
```
別 exec で `JSON.stringify(window.__t1,null,1)`。
Expected: `count=3`, `row0.note="メモA"`, `row1IsSpacer=true`, `collectedCount=3`, `collectedHasSpacer=true`, `note0="メモA"`, `afterInsertCount=4`, `afterInsertRow1Spacer=true`。

- [ ] **Step 8: Commit**

```bash
cd "/Users/shiromashin/Desktop/claudecode/sat/案件管理アプリ" && git add index.html && git commit -m "feat(inv): 明細に備考(note)と空行(spacer)のデータ層を追加

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 編集モーダルに備考欄 ＋ カードUI（備考表示・下に空行・空行カード）

**Files:** Modify `index.html`（モーダルHTML 6827 直後、`openInvLineModal` 10240、`saveInvLineModal` 10277、`renderInvLineCards` 10180）

- [ ] **Step 1: モーダルに備考 textarea を追加**

`index.html:6825-6828` の摘要フィールド：
```html
        <div class="inv-field">
          <label>摘要（作業内容）</label>
          <textarea id="inv-modal-name" placeholder="例）カメラマン人件費（撮影：城間）" oninput="_updateInvModalAmount()"></textarea>
        </div>
```
の**直後**に追加：
```html
        <div class="inv-field">
          <label>備考（プレビュー右端に表示）</label>
          <textarea id="inv-modal-note" placeholder="例）当日精算ぶんを含む"></textarea>
        </div>
```

- [ ] **Step 2: `openInvLineModal` で備考を復元**

`index.html:10259` の `document.getElementById('inv-modal-outsource').checked = !!l.outsource;` の直後に追加：
```javascript
    { const nEl = document.getElementById('inv-modal-note'); if (nEl) { nEl.value = l.note || ''; nEl.disabled = _invReadonly; } }
```

- [ ] **Step 3: `saveInvLineModal` で備考を保存**

`index.html:10280-10288` の `const data = {...}` に `note` を追加：
```javascript
    const data = {
      name:  document.getElementById('inv-modal-name').value.trim(),
      qty:   parseFloat(document.getElementById('inv-modal-qty').value) || 0,
      unit:  document.getElementById('inv-modal-unit').value.trim() || '式',
      price: parseFloat(document.getElementById('inv-modal-price').value) || 0,
      taxable:   document.getElementById('inv-modal-tax').value === '1',
      code:  document.getElementById('inv-modal-code').value.trim(),
      outsource: document.getElementById('inv-modal-outsource').checked,
      note:  (document.getElementById('inv-modal-note')?.value || '').trim()
    };
```
（`_writeInvLineRow` は Task1 で `tr.dataset.note=d.note` を書くので、これで保存される）

- [ ] **Step 4: `renderInvLineCards` に備考表示・下に空行ボタン・空行カード**

`index.html:10190-10215` の `wrap.innerHTML = rows.map((l, i) => {...}).join('');` を次に置換：
```javascript
    wrap.innerHTML = rows.map((l, i) => {
      if (l.spacer) {
        const delS = _invReadonly ? '' : `<button type="button" class="inv-card-del" title="空行を削除" onclick="delInvLineCard(${i})">🗑</button>`;
        return `<div class="inv-card" style="opacity:0.75;">
          <div class="inv-card-body" style="cursor:default;color:#999;font-size:12px;letter-spacing:2px;">— 空行（スペース）—</div>
          <div class="inv-card-right">${delS}</div>
        </div>`;
      }
      const amt = Math.round((l.qty || 0) * (l.price || 0));
      const nameTxt = l.name ? _esc(l.name) : '（摘要未入力）';
      const nameCls = l.name ? '' : ' is-empty';
      const taxBadge = l.taxable === false
        ? '<span class="inv-card-badge tax-off">非課税</span>'
        : '<span class="inv-card-badge">課税</span>';
      const outBadge = l.outsource ? '<span class="inv-card-badge outsrc">外注</span>' : '';
      const codeBadge = l.code ? `<span class="inv-card-badge">${_esc(l.code)}</span>` : '';
      const noteLine = l.note ? `<div class="inv-card-note" style="font-size:11px;color:#888;margin-top:3px;">📝 ${_esc(l.note)}</div>` : '';
      const delBtn  = _invReadonly ? '' :
        `<button type="button" class="inv-card-del" title="削除" onclick="delInvLineCard(${i})">🗑</button>`;
      const spacerBtn = _invReadonly ? '' :
        `<button type="button" class="inv-card-spacer-btn" title="この下に空行を入れる" onclick="insertSpacerAfter(${i})" style="font-family:'DotGothic16',monospace;font-size:11px;background:#fff;color:#2980b9;border:1px solid #2980b9;border-radius:4px;padding:3px 8px;cursor:pointer;margin-top:6px;">＋ 下に空行</button>`;
      return `
        <div class="inv-card">
          <button type="button" class="inv-card-body" onclick="openInvLineModal(${i})">
            <div class="inv-card-name${nameCls}">${nameTxt}</div>
            <div class="inv-card-sub">
              <span>¥${(l.price||0).toLocaleString()} × ${l.qty || 0}${_esc(l.unit||'')}</span>
              ${taxBadge}${outBadge}${codeBadge}
            </div>
            ${noteLine}
            ${spacerBtn}
          </button>
          <div class="inv-card-right">
            <div class="inv-card-amount">¥${amt.toLocaleString()}</div>
            ${delBtn}
          </div>
        </div>`;
    }).join('');
```
注意：`spacerBtn` は `.inv-card-body`（button）の中に置くと入れ子ボタンになるため、`.inv-card-body` を閉じた後に出す。上記は `.inv-card-body` 内に入れているので、**`spacerBtn` は `.inv-card-body` の外**へ移すこと。具体的には `</button>` の直後（`<div class="inv-card-right">` の前）に `${spacerBtn}` を置く。最終形：
```javascript
      return `
        <div class="inv-card">
          <button type="button" class="inv-card-body" onclick="openInvLineModal(${i})">
            <div class="inv-card-name${nameCls}">${nameTxt}</div>
            <div class="inv-card-sub">
              <span>¥${(l.price||0).toLocaleString()} × ${l.qty || 0}${_esc(l.unit||'')}</span>
              ${taxBadge}${outBadge}${codeBadge}
            </div>
            ${noteLine}
          </button>
          ${spacerBtn}
          <div class="inv-card-right">
            <div class="inv-card-amount">¥${amt.toLocaleString()}</div>
            ${delBtn}
          </div>
        </div>`;
```

- [ ] **Step 5: 構文チェック**

Run（共通）→ Expected: `OK`

- [ ] **Step 6: ブラウザで確認（サーバ再読込後）**

console：
```javascript
window.__t2='run';(async()=>{ const r={};
  try{
    _openBlankInvScreen('create');
    renderInvLines([], [{code:'',name:'撮影費',unit:'式',qty:1,price:5000,taxable:true,outsource:false,note:''}], false, 0);
    // モーダルで備考を編集
    openInvLineModal(0);
    document.getElementById('inv-modal-note').value='現場精算込み';
    saveInvLineModal();
    r.savedNote=_readInvLineRows()[0].note;                 // 現場精算込み
    // カードに備考が出るか
    r.cardHasNote=/現場精算込み/.test(document.getElementById('inv-cards').innerHTML);
    r.cardHasSpacerBtn=/下に空行/.test(document.getElementById('inv-cards').innerHTML);
    // 下に空行 → 空行カード
    insertSpacerAfter(0);
    r.spacerCard=/空行（スペース）/.test(document.getElementById('inv-cards').innerHTML);
    r.rowCount=_readInvLineRows().length;                   // 2
    try{closeInv();}catch(e){}
  }catch(e){ r.error=String(e&&e.stack||e); }
  window.__t2=r;
})();'started'
```
別 exec で `JSON.stringify(window.__t2,null,1)`。
Expected: `savedNote="現場精算込み"`, `cardHasNote=true`, `cardHasSpacerBtn=true`, `spacerCard=true`, `rowCount=2`。

- [ ] **Step 7: Commit**

```bash
cd "/Users/shiromashin/Desktop/claudecode/sat/案件管理アプリ" && git add index.html && git commit -m "feat(inv): 明細モーダルに備考欄、カードに備考表示と「下に空行」・空行カードを追加

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 印刷HTML — 行備考 ＋ 空行

**Files:** Modify `index.html`（`buildDocHTML` 明細ループ 14402-14434、thead 14543）

- [ ] **Step 1: 明細ループを spacer 対応＋備考表示に置換**

`index.html:14402-14434` の `document.querySelectorAll('.inv-line-row').forEach((tr, i) => {...});`（`if (!name) return;` を含むブロック）を次に置換：
```javascript
    document.querySelectorAll('.inv-line-row').forEach((tr, i) => {
      if (tr.dataset.spacer === '1') {
        linesHTML += `<tr class="doc-spacer-row"><td style="${tdS}height:14px;">&nbsp;</td>${`<td style="${tdS}"></td>`.repeat(5)}</tr>`;
        return;
      }
      const nameEl  = tr.querySelector('.inv-line-name') || tr.querySelector('.inv-td-name span');
      const unitEl  = tr.querySelector('.inv-line-unit') || tr.querySelector('.inv-td-unit span');
      const qtyEl   = tr.querySelector('.inv-line-qty')  || tr.querySelector('.inv-td-qty span');
      const priceEl = tr.querySelector('.inv-line-price');
      const priceTextEl = tr.querySelector('.inv-td-price span');
      const codeEl  = tr.querySelector('.inv-td-code');
      const taxEl   = tr.querySelector('.inv-line-tax');
      const amtEl   = document.getElementById('inv-amount-' + i);

      const name  = escapeHtml(nameEl?.value  || nameEl?.textContent?.trim()  || '');
      const unit  = escapeHtml(unitEl?.value  || unitEl?.textContent?.trim()  || '');
      const qty   = escapeHtml(qtyEl?.value   || qtyEl?.textContent?.trim()   || '');
      const rawPrice = priceEl?.value || priceTextEl?.textContent?.replace(/[¥,]/g, '') || '';
      const price = rawPrice ? Number(rawPrice).toLocaleString() : '';
      const amt   = amtEl?.textContent?.trim()  || '';
      const taxable = taxEl ? (taxEl.value === '1')
                            : (tr.querySelector('.inv-td-tax span')?.textContent?.trim() !== '非課税');
      const note  = escapeHtml(tr.dataset.note || '');
      const taxMark = taxable ? '' : '<div style="font-size:7.5pt;color:#c0392b;font-weight:bold;">非課税</div>';
      const noteCell = (note ? `<div>${note}</div>` : '') + taxMark;
      if (!name) return;
      rowCount++;
      const bg = rowCount % 2 === 0 ? '#f9f9f9' : 'white';
      linesHTML += `
        <tr style="background:${bg};">
          <td style="${tdS}">${name}</td>
          <td style="${tdS}text-align:right;">${qty}</td>
          <td style="${tdS}text-align:center;">${unit}</td>
          <td style="${tdS}text-align:right;">${price ? '¥'+price : ''}</td>
          <td style="${tdS}text-align:right;font-weight:bold;">${amt}</td>
          <td style="${tdS}text-align:left;font-size:7.5pt;">${noteCell}</td>
        </tr>`;
    });
```
（変更点：先頭に spacer 分岐、`note` を読み `noteCell` に備考＋非課税を併記、備考セルを左寄せ小文字に）

- [ ] **Step 2: 備考列の見出し幅を広げる**

`index.html:14543` の
```html
            <th style="${thS}width:60px;">備考</th>
```
を
```html
            <th style="${thS}width:90px;">備考</th>
```
に変更。

- [ ] **Step 3: 構文チェック** → Run（共通）→ Expected: `OK`

- [ ] **Step 4: ブラウザで印刷HTMLを確認（サーバ再読込後）**

`buildDocHTML` の出力文字列を直接検査（プレビューを開かずHTML生成のみ）：
```javascript
window.__t3='run';(async()=>{ const r={};
  try{
    _openBlankInvScreen('create');
    renderInvLines([], [
      {code:'',name:'撮影費',unit:'式',qty:2,price:5000,taxable:true,outsource:false,note:'現場精算込み'},
      {spacer:true},
      {code:'',name:'編集費',unit:'式',qty:1,price:8000,taxable:false,outsource:false,note:''}
    ], false, 0);
    calcInvTotal();
    const html=buildDocHTML('invoice', 3);
    r.hasNote=/現場精算込み/.test(html);          // true（備考が出る）
    r.hasSpacerRow=/doc-spacer-row/.test(html);    // true（空行が出る）
    r.hasHikazei=/非課税/.test(html);              // true（非課税マーカーも共存）
    // 合計は空行の影響を受けない：撮影10000(課税)+編集8000(非課税)=18000, 税=floor(10000*0.1)=1000, 合計19000
    r.total=(document.getElementById('inv-total-big')||{}).textContent;
    try{closeInv();}catch(e){}
  }catch(e){ r.error=String(e&&e.stack||e); }
  window.__t3=r;
})();'started'
```
別 exec で `JSON.stringify(window.__t3,null,1)`。
Expected: `hasNote=true`, `hasSpacerRow=true`, `hasHikazei=true`, `total="¥19,000"`。

- [ ] **Step 5: Commit**

```bash
cd "/Users/shiromashin/Desktop/claudecode/sat/案件管理アプリ" && git add index.html && git commit -m "feat(inv): 印刷/プレビューに行備考と空行を反映（備考は非課税表示と共存）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 印刷HTML — 3書類の見分け（色帯・アイコン・右上バッジ）

**Files:** Modify `index.html`（`buildDocHTML` の `_T` 14373-14377、返却HTML先頭 14463-14464、金額ブロック border 14526）

- [ ] **Step 1: `_T` に色・バッジ・アイコンを追加**

`index.html:14373-14377` の `_T` を次に置換（各typeに `barColor`/`badge`/`iconSvg` を追加）：
```javascript
    const _ICON = {
      invoice:  '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="vertical-align:-2px;"><path d="M5 21V4a1 1 0 0 1 1-1h9l4 4v14l-2-1-2 1-2-1-2 1-2-1-2 1z"/><path d="M9 8h6M9 12h6M9 16h4"/></svg>',
      estimate: '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="vertical-align:-2px;"><rect x="5" y="3" width="14" height="18" rx="1"/><path d="M8 7h8M8 11h2M12 11h2M16 11h0M8 15h2M12 15h2M16 15h0"/></svg>',
      delivery: '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="vertical-align:-2px;"><path d="M3 8l9-5 9 5v8l-9 5-9-5z"/><path d="M3 8l9 5 9-5M12 13v8"/></svg>',
    };
    const _T = {
      invoice:  { title: '御　請　求　書', date: '御請求日', amt: '御請求金額', lead: '下記の通りご請求申し上げます。', barColor:'#1f9e6f', badge:'請 求 書', iconSvg:_ICON.invoice },
      estimate: { title: '御　見　積　書', date: '御見積日', amt: '御見積金額', lead: '下記の通り御見積り申し上げます。', barColor:'#d9791d', badge:'見 積 書', iconSvg:_ICON.estimate },
      delivery: { title: '御　納　品　書', date: '納品日',   amt: '御納品金額', lead: '下記の通り御納品申し上げます。', barColor:'#2377ba', badge:'納 品 書', iconSvg:_ICON.delivery },
    };
```

- [ ] **Step 2: 追加の定数を取り出す**

`index.html:14378-14382`（`const _L = _T[type] || _T.invoice;` 以降）の直後に追加：
```javascript
    const barColor = _L.barColor || '#1f9e6f';
    const badgeTxt = _L.badge || '請 求 書';
    const iconSvg  = _L.iconSvg || '';
```

- [ ] **Step 3: 返却HTMLの先頭（1行目：空白）を色帯＋バッジに差し替え**

`index.html:14463-14464` の
```html
      <!-- 1行目：空白 -->
      <div style="height:4mm;"></div>
```
を次に置換：
```html
      <!-- 書類種別バー（色＝画面/カラー印刷・右上バッジ＋アイコン＝白黒でも判別） -->
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:3mm;">
        <div style="background:${barColor};color:#fff;padding:1.5mm 4mm;font-size:11pt;font-weight:bold;letter-spacing:2px;border-radius:1mm;display:flex;align-items:center;gap:2mm;print-color-adjust:exact;-webkit-print-color-adjust:exact;">
          ${iconSvg}<span>${badgeTxt}</span>
        </div>
        <div style="border:2px solid #111;border-radius:1mm;padding:1mm 4mm;font-size:11pt;font-weight:bold;letter-spacing:2px;">${badgeTxt}</div>
      </div>
```

- [ ] **Step 4: 金額ブロックの枠色を書類色に合わせる（カラーの補強）**

`index.html:14526-14531` の金額ブロックの `border:1.5px solid #2980b9;` と `color:#2980b9;` を書類色に。該当ブロックを次に置換：
```html
      <!-- 御請求金額ブロック -->
      <div style="display:flex;align-items:center;justify-content:space-between;
        border:1.5px solid ${barColor};padding:3mm 5mm;margin-bottom:3mm;background:#fafafa;print-color-adjust:exact;-webkit-print-color-adjust:exact;">
        <div style="font-size:9pt;color:${barColor};font-weight:bold;">${amtLabel}</div>
        <div style="font-size:16pt;font-weight:bold;">${total}</div>
        <div style="font-size:8pt;color:#555;">内消費税(10%)　${tax}</div>
      </div>
```

- [ ] **Step 5: 構文チェック** → Run（共通）→ Expected: `OK`

- [ ] **Step 6: ブラウザで3書類の差を確認（サーバ再読込後）**

console：
```javascript
window.__t4='run';(async()=>{ const r={};
  try{
    _openBlankInvScreen('create');
    renderInvLines([], [{code:'',name:'撮影費',unit:'式',qty:1,price:5000,taxable:true,outsource:false,note:''}], false, 0);
    calcInvTotal();
    const inv=buildDocHTML('invoice',3), est=buildDocHTML('estimate',3), dlv=buildDocHTML('delivery',3);
    r.invColor=/#1f9e6f/.test(inv); r.estColor=/#d9791d/.test(est); r.dlvColor=/#2377ba/.test(dlv);
    r.invBadge=/請 求 書/.test(inv); r.estBadge=/見 積 書/.test(est); r.dlvBadge=/納 品 書/.test(dlv);
    r.hasIcon=/<svg/.test(inv) && /<svg/.test(est) && /<svg/.test(dlv);   // 白黒でも出るアイコン
    r.blackBadgeBorder=/border:2px solid #111/.test(inv);                 // 白黒判別用バッジ
    try{closeInv();}catch(e){}
  }catch(e){ r.error=String(e&&e.stack||e); }
  window.__t4=r;
})();'started'
```
別 exec で `JSON.stringify(window.__t4,null,1)`。
Expected: 全て `true`（各色・各バッジ・SVGアイコン・黒枠バッジ）。

- [ ] **Step 7: 実プレビューで目視（1回）**

ログイン後（PIN 城間/0000）、請求書を新規作成→🔍プレビュー→請求書/見積書/納品書をモード切替してプレビューし、帯色・アイコン・右上バッジが変わることをスクショで確認。白黒確認はブラウザ印刷ダイアログの「グレースケール」またはデベロッパツールで `filter:grayscale(1)` を body に当てて、バッジ＋アイコンで判別できることを確認。

- [ ] **Step 8: Commit**

```bash
cd "/Users/shiromashin/Desktop/claudecode/sat/案件管理アプリ" && git add index.html && git commit -m "feat(inv): 請求書/見積書/納品書を色帯＋アイコン＋右上バッジで見分け（白黒対応）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: テンプレ引き継ぎ ＋ 統合テスト ＋ UI4観点 ＋ push

**Files:** Modify `index.html`（`saveCurrentAsTemplate` の lines マップ、`createInvoiceFromTemplate` の tplLines マップ）＋ 検証

- [ ] **Step 1: テンプレ保存で note / spacer を残す**

`saveCurrentAsTemplate`（`function saveCurrentAsTemplate()`）の `lines: c.lines.map(l => ({ ... }))` を次に置換（note・spacer を追加、数量/単価は空のまま）：
```javascript
      lines: c.lines.map(l => ({ code: l.code || '', name: l.name || '', unit: l.unit || '',
                                 qty: '', price: '', taxable: l.taxable !== false, outsource: !!l.outsource,
                                 note: l.note || '', spacer: !!l.spacer })),
```

- [ ] **Step 2: テンプレから作成で note / spacer を引き継ぐ**

`createInvoiceFromTemplate`（`function createInvoiceFromTemplate(id)`）の `const tplLines = (tpl.lines && tpl.lines.length) ? tpl.lines.map(l => ({ ... })) : [...]` の map を次に置換：
```javascript
      ? tpl.lines.map(l => ({ code: l.code || '', name: l.name || '', unit: l.unit || '',
                              qty: '', price: '', taxable: l.taxable !== false, outsource: !!l.outsource,
                              note: l.note || '', spacer: !!l.spacer }))
```

- [ ] **Step 3: 構文チェック** → Run（共通）→ Expected: `OK`

- [ ] **Step 4: 一気通貫E2E（保存→プレビュー→テンプレ→作成→既存互換）**

サーバ再読込後 console：
```javascript
window.__t5='run';(async()=>{ const sleep=ms=>new Promise(r=>setTimeout(r,ms)); const r={};
  const op=window.prompt,oa=window.alert,ourl=localStorage.getItem('gas_url');
  window.prompt=()=> 'doc改良テンプレ'; window.alert=()=>{}; localStorage.removeItem('gas_url');
  try{
    saveInvTemplates([]);
    _openBlankInvScreen('create');
    const num=(document.getElementById('inv-detail-num').textContent||'').trim();
    renderInvLines([], [
      {code:'',name:'撮影費',unit:'式',qty:2,price:5000,taxable:true,outsource:false,note:'現場精算込み'},
      {spacer:true},
      {code:'',name:'編集費',unit:'式',qty:1,price:8000,taxable:false,outsource:false,note:''}
    ], false, 0);
    document.getElementById('inv-screen-company').value='通しテスト社';
    document.getElementById('inv-screen-title').value='通しテスト';
    calcInvTotal();
    r.total=(document.getElementById('inv-total-big')||{}).textContent;   // ¥19,000
    // テンプレ保存
    saveCurrentAsTemplate();
    const tpl=loadInvTemplates()[0];
    r.tplHasSpacer=(tpl.lines||[]).some(l=>l.spacer);                     // true
    r.tplHasNote=(tpl.lines||[]).some(l=>l.note==='現場精算込み');        // true
    r.tplQtyEmpty=(tpl.lines.find(l=>l.name==='撮影費')||{}).qty==='';    // true
    // 通常保存→再取得で note/spacer が残る
    saveInvDoc(); await sleep(200);
    const st=JSON.parse(localStorage.getItem('inv_'+num)||'{}');
    r.savedHasSpacer=(st.lines||[]).some(l=>l.spacer);                    // true
    r.savedHasNote=(st.lines||[]).some(l=>l.note==='現場精算込み');       // true
    try{closeInv();}catch(e){}
    // テンプレから作成→note/spacer 復元・数量空
    createInvoiceFromTemplate(tpl.id);
    const rows=_readInvLineRows();
    r.fromTplSpacer=rows.some(l=>l.spacer);                               // true
    r.fromTplNote=rows.some(l=>l.note==='現場精算込み');                  // true
    try{closeInv();}catch(e){}
    // 既存互換：note/spacer 無しデータでも壊れない
    renderInvLines([], [{code:'',name:'旧データ',unit:'式',qty:1,price:1000,taxable:true,outsource:false}], false, 0);
    r.legacyOk=_readInvLineRows()[0].name==='旧データ' && buildDocHTML('invoice',3).indexOf('旧データ')>=0;
    try{closeInv();}catch(e){}
  }catch(e){ r.error=String(e&&e.stack||e); }
  finally{ window.prompt=op; window.alert=oa; if(ourl==null)localStorage.removeItem('gas_url'); else localStorage.setItem('gas_url',ourl);
    try{Object.keys(localStorage).filter(k=>/^inv_T26/.test(k)).forEach(k=>{}); }catch(e){} saveInvTemplates([]); }
  window.__t5=r;
})();'started'
```
別 exec で `JSON.stringify(window.__t5,null,1)`。
Expected: `total="¥19,000"`, `tplHasSpacer=true`, `tplHasNote=true`, `tplQtyEmpty=true`, `savedHasSpacer=true`, `savedHasNote=true`, `fromTplSpacer=true`, `fromTplNote=true`, `legacyOk=true`。

- [ ] **Step 5: UI 4観点（実機・目視）**

ログイン後、請求書編集画面で **390px／デスクトップ × font_scale 1.0／1.5** の4パターン：
1. 明細編集モーダルに「備考」欄が出て崩れない
2. カードの「＋ 下に空行」ボタン・空行カード・備考行が崩れない
3. モーダル・カードが拡大時(150%)もはみ出さない
（測定は前回同様、必要なら要素を390px枠に複製して `scrollWidth<=clientWidth` を確認）。報告に「✅ 4観点OK」を明記。

- [ ] **Step 6: 最終構文チェック** → Run（共通）→ Expected: `OK`

- [ ] **Step 7: Commit & push**

```bash
cd "/Users/shiromashin/Desktop/claudecode/sat/案件管理アプリ" && git add index.html && git commit -m "feat(inv): テンプレに行備考・空行を引き継ぎ＋統合確認" && git push 2>&1 | tail -3; lsof -ti:8793 2>/dev/null | xargs kill 2>/dev/null
```
報告に「✅ 構文OK」「✅ E2E（備考/空行/合計不変/テンプレ/既存互換）」「✅ 3書類の見分け（色＋白黒）」「✅ 4観点OK」「✅ push完了（ハッシュ）」を明記。

---

## 自己レビュー（writing-plans）

**1. 仕様カバレッジ:**
- ① 空行：Task1（データ層・insertSpacerAfter）＋Task2（下に空行ボタン・空行カード）＋Task3（印刷で空欄行）＋Task5（テンプレ引き継ぎ）✓
- ② 行備考：Task1（note保管）＋Task2（モーダル欄・カード表示）＋Task3（備考列に表示・非課税と共存）＋Task5（テンプレ引き継ぎ）✓
- ③ 見分け：Task4（色帯＋アイコン＋バッジ＋金額枠色、白黒はバッジ＋SVGアイコン）✓
- データモデル note/spacer・後方互換：Task1＋Task5 legacyOk で確認 ✓
- 集計不変：Task3/Task5 で total=¥19,000 を確認 ✓
- 保存/同期：_collectInvLines 経由で自動（Task1）、確認は Task5 ✓
- UI4観点：Task5 Step5 ✓

**2. プレースホルダ:** なし（全ステップに実コード/実コマンド）。

**3. 型/名前の一貫性:** 行データのフィールド `{code,name,unit,qty,price,taxable,outsource,note,spacer}` を全タスクで統一。関数 `insertSpacerAfter`/`_readInvLineRows`/`_collectInvLines`/`renderInvLines`/`renderInvLineCards`/`buildDocHTML`/`saveCurrentAsTemplate`/`createInvoiceFromTemplate` は既存名を踏襲。dataset キーは `note`/`spacer`（'1'）で統一。

---
最終更新：2026-07-03
